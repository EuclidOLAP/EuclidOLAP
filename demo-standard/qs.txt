select
children([Aircraft Models].[ALL]) on rows,
children([Date].[ALL]) on columns
from [Airline A]
where ([Measures].Revenue);

---

select
{
    ClosingPeriod([Date].[Calendar].Month, [Date].[2021]),
    [Date].[2021].Q4.M12,
    ClosingPeriod([Date].[Calendar].Month),
    [Date].[2022].Q4.M12
} on 0,
{
  [Measures].Revenue,
  [Measures].Cost
} on rows
from [Airline A];